{
  "name": "node-red-server",
  "version": "1.0.0",
  "description": "Node-RED on Render",
  "scripts": {
    "start": "npx node-red -p $PORT --userDir ./data"
  },
  "dependencies": {
    "node-red": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
